#include<stdio.h>
#include<string.h>
void cmp(char *x,char *y)
{
    char temp[100];
    if(strcmp(*x,*y)>0)
    {
        s
    }
}
int main()
{
    char a[100],b[100],c[100];
    gets(a);
    gets(b);
    gets(c);

}
